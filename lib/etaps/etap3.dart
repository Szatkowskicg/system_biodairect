import 'package:flutter/material.dart';

class Etap3 extends StatelessWidget {
  const Etap3({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Etap3'),
      ),
    );
  }
}